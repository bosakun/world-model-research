# Outputs

`mpc_rollout.png` shows the executed feedback path and Goal distance after each replanning call. `evaluation_metrics.json` records success/configuration. Regenerate with `.venv/bin/python 07_planning/03_mpc/evaluate.py`.
