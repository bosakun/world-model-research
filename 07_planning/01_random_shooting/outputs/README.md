# Outputs

`random_shooting_plan.png` shows the selected exact-model path and candidate return histogram. `evaluation_metrics.json` records the reproducible configuration and outcome. Regenerate with `.venv/bin/python 07_planning/01_random_shooting/evaluate.py`.
